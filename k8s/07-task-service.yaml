apiVersion: v1
kind: Service
metadata:
  name: task-service
  namespace: cloud-task-app
spec:
  selector:
    app: task-service

  ports:
    - port: 80
      targetPort: 5002
      nodePort: 30080

  type: NodePort
